## Rails App using DelayedJob

To launch the DelayedJob worker run:

```
bundle exec rake jobs:work
```

Launch Rails:

```
bundle exec rails s
```

Visit http://localhost:3000/emails and click "New Email"

Create Email